
This is the dedication tex file, which should have been set in the main file using the command \verb|\setDedicationFile{Drive:/file/1.OpeninPages/dedicationFile}|. It should begin with the word “To” and is typically not a full sentence, e.g. "To Mom"